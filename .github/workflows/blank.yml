on: [push]

jobs:
  influxdb_job:
    runs-on: ubuntu-latest
    name: A job to set up InfluxDB
    steps:
      - name: Check out repo
        uses: actions/checkout@v2
      - name: Setup InfluxDB
        uses: russorat/influxdb-action@0.0.2
